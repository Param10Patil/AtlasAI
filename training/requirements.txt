# Optional training environment; never install this in the runtime image.
# This exact set was selected for Python 3.11 CPU/Colab compatibility on
# 2026-09-08. Keep it separate from the small runtime image.
torch==2.3.1
transformers==4.43.3
peft==0.12.0
datasets==2.20.0
accelerate==0.33.0
numpy==1.26.4
safetensors==0.4.3
mlflow==2.15.1
