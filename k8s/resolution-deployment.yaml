apiVersion: apps/v1
kind: Deployment
metadata:
  name: resolution
  namespace: opspilot
spec:
  replicas: 1
  selector:
    matchLabels: {app: opspilot-resolution}
  template:
    metadata: {labels: {app: opspilot-resolution}}
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: resolution
          image: opspilot:dev
          command: [python, -m, app.run, --role, resolution]
          resources:
            requests: {cpu: 50m, memory: 96Mi}
            limits: {cpu: 500m, memory: 384Mi}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: {drop: [ALL]}
          readinessProbe: {httpGet: {path: /api/ready, port: 8080}, periodSeconds: 5}
          livenessProbe: {httpGet: {path: /api/health, port: 8080}, periodSeconds: 10}
